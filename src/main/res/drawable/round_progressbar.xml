<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape android:useLevel="false"
            android:shape="ring"
            android:innerRadiusRatio="@dimen/timer_progressbar_inner_radius_background"
            android:thickness="@dimen/timer_progressbar_thickness_background">
            <solid android:color="@color/timer_progressbar_background" />
            <corners android:radius="50dp"/>
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <shape
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:useLevel="true"
            android:shape="ring"
            android:innerRadiusRatio="@dimen/timer_progressbar_inner_radius"
            android:thickness="@dimen/timer_progressbar_thickness">
            <solid android:color="@color/timer_progressbar" />
        </shape>
        <scale
              android:drawable="@drawable/round_progressbar"
              android:scaleWidth="98%" />
    </item>
</layer-list>